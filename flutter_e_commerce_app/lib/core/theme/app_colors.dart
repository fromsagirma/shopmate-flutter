import 'package:flutter/material.dart';

class AppColors {
  // Primary palette (Deep Indigo)
  static const Color primary = Color(0xFF4F46E5);
  static const Color onPrimary = Colors.white;
  static const Color primaryContainer = Color(0xFFE0E7FF);
  static const Color onPrimaryContainer = Color(0xFF312E81);

  // Backgrounds & Surfaces (Off-white / Slate)
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color onSurface = Color(0xFF0F172A); // High-contrast Slate
  static const Color onSurfaceVariant = Color(0xFF64748B); // Muted gray

  // Borders & Dividers
  static const Color outline = Color(0xFFE2E8F0);
  
  // Status Colors
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
}